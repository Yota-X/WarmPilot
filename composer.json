{
  "name": "yota-x/warmpilot",
  "description": "Runtime dependencies, development tooling, tests, and release build for the WarmPilot WordPress plugin.",
  "type": "wordpress-plugin",
  "license": "GPL-2.0-or-later",
  "require": {
    "php": ">=8.1",
    "ext-dom": "*",
    "ext-libxml": "*",
    "ext-simplexml": "*",
    "jeremykendall/php-domain-parser": "^6.4",
    "symfony/polyfill-intl-idn": "^1.38"
  },
  "require-dev": {
    "phpunit/phpunit": "^9.6"
  },
  "scripts": {
    "test": [
      "@test:php",
      "@test:js"
    ],
    "test:php": "phpunit --configuration phpunit.xml",
    "test:js": "node --test test/Js/*.test.js",
    "test:syntax": [
      "@php -r \"$it=new RecursiveIteratorIterator(new RecursiveDirectoryIterator(__DIR__, FilesystemIterator::SKIP_DOTS)); foreach($it as $f){if($f->getExtension()==='php' && !str_contains($f->getPathname(), DIRECTORY_SEPARATOR.'vendor'.DIRECTORY_SEPARATOR)){passthru(escapeshellarg(PHP_BINARY).' -l '.escapeshellarg($f->getPathname()), $c); if($c) exit($c);}}\"",
      "node --check assets/admin.js"
    ],
    "update:licenses": "@php build/update-license-texts.php",
    "update:psl": "@php build/update-public-suffix-list.php",
    "build": [
      "@test:syntax",
      "@test",
      "@composer install --no-dev --classmap-authoritative --no-interaction --no-progress",
      "@php build/build.php",
      "@composer install --no-interaction --no-progress"
    ]
  },
  "config": {
    "sort-packages": true,
    "allow-plugins": {}
  }
}
